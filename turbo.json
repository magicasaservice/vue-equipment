{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist/**"]
    },
    "build:types": {
      "cache": false,
      "outputs": ["./types/**"]
    },
    "//#metadata:update": {
      "cache": false,
      "outputs": ["./packages/metadata/index.json"]
    },
    "//#t:release": {
      "dependsOn": ["^build", "nuxt#build"]
    },
    "//#t:docs:dev": {
      "dependsOn": ["//#metadata:update", "^build", "^build:types"]
    },
    "//#t:docs:build": {
      "dependsOn": ["//#metadata:update", "^build", "^build:types"]
    },
    "nuxt#build": {
      "dependsOn": ["//#metadata:update", "composables#build", "plugins#build"]
    },
    "nuxt#dev": {
      "dependsOn": ["//#metadata:update", "composables#build", "plugins#build"]
    }
  }
}
