import type { MagicPlayerOptions } from '../types'

export const audioModePlaybackDefaults: MagicPlayerOptions['playback'] = [
  'viewport',
]

export const videoModePlaybackDefaults: MagicPlayerOptions['playback'] = [
  'viewport',
  'window',
]
