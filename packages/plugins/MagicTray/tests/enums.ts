export enum TrayId {
  // rendering
  RenderInline = 'render-inline',
  RenderHandles = 'render-handles',
  RenderNoHandles = 'render-no-handles',
  RenderPartialHandles = 'render-partial-handles',
  RenderDisabled = 'render-disabled',
  // api
  ApiSnap = 'api-snap',
  // interactions
  Overshoot = 'overshoot',
  // events
  EventSnap = 'event-snap',
}

export enum TestId {
  Content = 'content',
  SnapBtn = 'snap-btn',
  ProgressBottom = 'progress-bottom',
  ProgressRight = 'progress-right',
  DraggedBottom = 'dragged-bottom',
  Events = 'events',
}
