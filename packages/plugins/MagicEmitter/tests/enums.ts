export enum TestId {
  EmitBtn = 'emit-btn',
  Btn = 'btn',
  Toggle = 'toggle',
  Emit = 'emit',
  SendBtn = 'send-btn',
  Received = 'received',
  ChildBtn = 'child-btn',
  ParentReceived = 'parent-received',
}
