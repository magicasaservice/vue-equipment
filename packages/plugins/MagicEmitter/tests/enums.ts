// ─── TestId ───────────────────────────────────────────────────────────────────
// Merged from all MagicEmitter test files

export enum TestId {
  // api.test.ts
  EmitBtn = 'emit-btn',
  Btn = 'btn',
  // edge-cases.test.ts
  Toggle = 'toggle',
  Emit = 'emit',
  // integration.test.ts
  SendBtn = 'send-btn',
  Received = 'received',
  ChildBtn = 'child-btn',
  ParentReceived = 'parent-received',
}
