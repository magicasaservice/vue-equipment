export enum ModeDelayMouseenter {
  menubar = 0,
  dropdown = 0,
  context = 0,
  navigation = 0,
}

export enum ModeDelayMouseleave {
  navigation = 100,
}

export enum ModeDelayClick {
  menubar = 0,
  dropdown = 0,
  context = 0,
  navigation = 0,
}
