export enum ModeDelayMouseenter {
  menubar = 0,
  dropdown = 0,
  context = 0,
  navigation = 50,
}

export enum ModeDelayMouseleave {
  navigation = 200,
}

export enum ModeDelayClick {
  menubar = 0,
  dropdown = 0,
  context = 0,
  navigation = 0,
}
