import type { MagicDotMatrixOptions } from '../types/index'

const defaultOptions: Required<MagicDotMatrixOptions> = {
  cols: 7,
  rows: 7,
  interval: 100,
  loop: true,
  autoplay: true,
}

export { defaultOptions }
